package Stream_API;


import java.util.*;
import java.util.stream.IntStream;

public class SWAP_INDEX_ARRAY {

    public static void main(String[] args) {
        int[] array = {1, 2, 3, 4, 5};

        // Swap elements at 1st and 3rd positions
//        int[] swappedArray = IntStream.range(0, array.length)
//                .map(i -> i == 0 ? array[2] : i == 2 ? array[0] : array[i])
//                .toArray();

//        Object[] array1 = Arrays.stream(array).boxed().map(i -> i == 0 ? array[2] : i == 2 ? array[0] : array[i]).toArray();
//
//        System.out.println("Original Array: " + Arrays.toString(array));
//        System.out.println("Swapped Array: " + Arrays.toString(array1));

        int x = 60;
        int y = 20;
        int z = 40;
//
//        System.out.println( x<y?x:y:x>z?x:z);

    }
}
